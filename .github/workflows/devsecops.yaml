name: devsecops

on:
    push:
        branches: [devsecops]
jobs:
    code-quality:
          uses: ./.github/workflows/code-quality.yaml
    secret-scanning:
           uses: ./.github/workflows/secert-scanning.yaml
           
          
